# Web app + Postgres (Render / production). Keep this lean for fast installs.
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.5
python-dotenv==1.0.0
requests==2.31.0
gunicorn==22.0.0
stripe==11.1.0
# Wheels for common Python versions; use runtime.txt to avoid 3.14 source builds.
psycopg[binary]==3.3.3
